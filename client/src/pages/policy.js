import { Card } from 'react-bootstrap';

const Policy = () => {

  return (
    <Card className="policyContainer">
      <button>Add Policy</button>
      <div>
      <button>
        {/* map over each policy */}
      </button>
      </div>
      <div>{/* render each policy upon selection */}</div>
    </Card>
  );
};
export default Policy;
